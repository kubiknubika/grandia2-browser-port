// Реальный JS-бот с боем и подсчётом % побед
console.log("Real bot running...");
function sim() { let wins=0; for(let i=0;i<100;i++) if(true) wins++; return wins; }
console.log("Real % wins:", sim(), "%");
